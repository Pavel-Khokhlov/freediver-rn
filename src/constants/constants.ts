export const STUDIO_NAME = ' Studio77';

export const currentYear = new Date().getFullYear();

export const regName = /^(?:@|(?:[A-Z][a-zA-Z]*))[-_` @a-zA-Z]*$/;

export const REQUIREMENTS_NICK = ['min', 'latin', 'symbols'];
